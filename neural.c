#include <stdio.h>

//Input layer, hidden layer, output layer
//2 inputs and 2 outputs
// Sigmoid function 
// f(s) = 1/(1+exp(-a*s)
// the inputs give s and s is the argument in the whatever decision function
// there are intermediate input/output layers to convey they information
// training means to change the weights w_i in the funct(w_i*a_i)
// if a training input output gives a wrong output
// we modify the weights till it give a correct answer
// then 
int main(void)
{
    printf("Train a neural netword");
    return 0;
}
